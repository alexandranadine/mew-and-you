# Cloudflare Pages routing for the Mew & You SPA.
# Docs: https://developers.cloudflare.com/pages/configuration/redirects/
#
# Order matters: first match wins.
#
# /api/* is proxied by the Pages Function at functions/api/[[path]].js
# (set API_ORIGIN=https://YOUR-RENDER-URL.onrender.com in the Pages dashboard).
# _routes.json limits Function invocations to /api/*; Functions are not subject
# to these redirect rules.

# SPA fallback — client routes (/, /cats, /about, …) serve index.html.
/* /index.html 200
